name: lamd_app
description: App do cliente LAMD — Sistema de chamados técnicos
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: ^2.3.0
  sqflite: ^2.3.3+1
  path: ^1.9.0
  uuid: ^4.5.0
  connectivity_plus: ^6.1.4
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
